{
  "name": "create-bebp",
  "version": "1.0.0",
  "description": "Boilerplate project for building UI with logic components.",
  "main": "dist/index.js",
  "bin": {
    "create-bebp": "./dist/cli.js"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build-element": "tsx buildElement"
  },
  "devDependencies": {
    "typescript": "latest",
    "vite": "latest",
    "vite-plugin-node-polyfills": "^0.23.0"
  },
  "dependencies": {
    "builder-settings-types": "^0.0.35",
    "element_interfaces": "^0.0.7",
    "esbuild": "^0.25.0"
  }
}
